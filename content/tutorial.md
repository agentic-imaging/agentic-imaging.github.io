# Tutorial

A short screencast will walk through one full AgX session end to end — posing an imaging problem,
watching the agents plan, implement, run, and verify it, and reading the provenance-locked
result — so the workflow is legible without installing anything.

**Tutorial video — pending.** It is recorded and captioned as part of the release (the roadmap's
V-series) and embedded here as a self-hosted file with a transcript; no third-party video player or
network request is introduced. Until it ships, the interactive workspace and trace demos show the
same end-to-end flow.

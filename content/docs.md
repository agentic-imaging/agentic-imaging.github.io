# Documentation (pending)

Versioned documentation ships with the release and is executable where possible — quickstarts
are tested end to end, and every CLI/API snippet is checked against the released code. The
planned organization:

- **Install & doctor** — local setup, environment checks, GPU detection.
- **Zero-to-first-run** — replay a bundled run without any credentials, then a live run.
- **Concepts** — the two modes, verifier stack, provenance model.
- **Deployment guides** — local, GPU workstation, cluster; sandboxing and data-handling policy.
- **Reference** — CLI, configuration schema, extension points, troubleshooting.
